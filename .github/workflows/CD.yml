name: deployment

on:
  release:
    types:
      - published
  workflow_dispatch:

jobs:
  publish:
    runs-on: ubuntu-latest
    steps:
      
    - name: Checkout code
      uses: actions/checkout@v3
    
    - name: Setup .NET
      uses: actions/setup-dotnet@v3
      with:
        dotnet-version: 7.0.x

    - name: Pack
      run: dotnet pack ./src/BetfairDotNet/BetfairDotNet.csproj --configuration Release -o ./out
  
    - name: Push to NuGet
      run: dotnet nuget push "./out/BetfairDotNet.*.nupkg" -k ${{ secrets.NUGET_API_KEY }} -s https://api.nuget.org/v3/index.json
